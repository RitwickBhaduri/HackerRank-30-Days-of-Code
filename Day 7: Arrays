#include <bits/stdc++.h>

using namespace std;



int main()
{
    int n,b[1000];
    cin>>n;
    for(int i=0;i<n;i++)
    {
        cin>>b[i];
    }
    for(int i=n-1;i>=0;i--)
    {
        cout<<b[i]<<" ";
    }
    return 0;
}
